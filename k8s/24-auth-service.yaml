apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: saude-poc
  labels: { app: auth-service }
spec:
  ports: [{ name: http, port: 8085, targetPort: 8085 }]
  selector: { app: auth-service }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: saude-poc
  labels: { app: auth-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: auth-service }
  template:
    metadata:
      labels: { app: auth-service, version: v1 }
    spec:
      containers:
        - name: auth-service
          image: saude-publica-poc-auth-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8085 }]
          env:
            - { name: SPRING_DATASOURCE_URL, value: "jdbc:postgresql://postgres:5432/authdb" }
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom: { configMapKeyRef: { name: shared-config, key: POSTGRES_USER } }
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom: { secretKeyRef: { name: shared-secrets, key: POSTGRES_PASSWORD } }
            - name: AUTH_JWT_SECRET
              valueFrom: { secretKeyRef: { name: shared-secrets, key: AUTH_JWT_SECRET } }
            - name: AUTH_JWT_ISSUER
              valueFrom: { configMapKeyRef: { name: shared-config, key: AUTH_JWT_ISSUER } }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8085 }
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8085 }
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits:   { cpu: "1000m", memory: "1Gi" }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: auth-service
  namespace: saude-poc
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: auth-service }
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
