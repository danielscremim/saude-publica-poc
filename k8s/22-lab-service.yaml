apiVersion: v1
kind: Service
metadata:
  name: lab-service
  namespace: saude-poc
  labels: { app: lab-service }
spec:
  ports: [{ name: http, port: 8083, targetPort: 8083 }]
  selector: { app: lab-service }
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lab-service
  namespace: saude-poc
  labels: { app: lab-service }
spec:
  replicas: 2
  selector:
    matchLabels: { app: lab-service }
  template:
    metadata:
      labels: { app: lab-service, version: v1 }
    spec:
      containers:
        - name: lab-service
          image: saude-publica-poc-lab-service:latest
          imagePullPolicy: IfNotPresent
          ports: [{ containerPort: 8083 }]
          env:
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              valueFrom: { configMapKeyRef: { name: shared-config, key: SPRING_KAFKA_BOOTSTRAP_SERVERS } }
          readinessProbe:
            httpGet: { path: /actuator/health/readiness, port: 8083 }
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /actuator/health/liveness, port: 8083 }
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits:   { cpu: "1000m", memory: "1Gi" }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: lab-service
  namespace: saude-poc
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: lab-service }
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: { type: Utilization, averageUtilization: 70 }
